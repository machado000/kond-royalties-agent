services:
  kond-royalties-mcp:
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
    networks:
      - default
      - kern-prefect_default
    restart: unless-stopped

networks:
  kern-prefect_default:
    external: true
